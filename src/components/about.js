export function initializeAbout() {
  // No stats to animate anymore, but keeping the function for consistency
  // and potential future enhancements
  
  // Add any other about section interactions here if needed
  console.log('About section initialized');
}